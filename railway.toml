[build]
builder = "NIXPACKS"
# Build is handled by nixpacks.toml

[deploy]
startCommand = "/app/venv/bin/python backend/main.py"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
